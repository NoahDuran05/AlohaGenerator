from setuptools import setup, find_packages

setup(
    name="aloha_blog",  # The name of your package
    version="1.1.0",  # Initial version
    packages=find_packages(),  # Automatically find all packages/subpackages
)
