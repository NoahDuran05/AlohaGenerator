[project]
name = "aloha_api"
version = "1.1.0"
description = "API facades"
authors = [{name = "Aloha Agentics", email = "mittul@alohaagentics.org"}]
dependencies = [
    "requests", "aloha-blog", "pydantic", "typing"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
